(()=>{const cfg=window.JEEBLI_CONFIG;if(!cfg||!window.supabase)return;const gateDb=window.supabase.createClient(cfg.SUPABASE_URL,cfg.SUPABASE_KEY);let busy=false;async function check(user){if(!user||busy)return;const {data}=await gateDb.from('profiles').select('role').eq('id',user.id).maybeSingle();if(data?.role==='admin'){busy=true;window.location.replace('./admin.html')}}gateDb.auth.getSession().then(({data})=>check(data.session?.user));gateDb.auth.onAuthStateChange((_event,session)=>check(session?.user))})();